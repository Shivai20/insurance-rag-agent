# --- Core LangChain (v1 line, verified mutually compatible) ---
langchain==1.3.6
langchain-core==1.4.3
langchain-openai==1.3.0
langchain-text-splitters==1.1.2

# --- Vector store (local, file-based; no server) ---
langchain-chroma==1.1.0
chromadb==1.5.9

# --- Agentic state machine ---
langgraph==1.2.4
langgraph-checkpoint==4.1.1

# --- Durable memory: Postgres checkpointer (used only if DATABASE_URL is set) ---
langgraph-checkpoint-postgres==3.1.0
psycopg[binary]==3.2.3
psycopg-pool==3.2.4

# --- Utils ---
pydantic==2.10.4
python-dotenv==1.0.1
tabulate==0.9.0

# --- Optional: LangSmith tracing (observability). Works with zero code changes. ---
langsmith==0.8.12

#--Frontend
streamlit==1.41.1